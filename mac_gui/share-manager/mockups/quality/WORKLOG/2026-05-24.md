# Worklog · 2026-05-24

Wave A kickoff for the Mac v0.3 mirror of Windows' git dashboard + quality
framework. Source-of-truth references for today:

- `mac_gui/MAC_MIRROR_PLAN.md` (Windows-authored, 336 lines, canonical)
- `mac_gui/WINDOWS_PARITY_BRIEF.md §18` (v0.3 git UI contract)
- `windows_gui/share-manager/mockups/quality/{CHECKLIST.md, ADR/0001..0004}`

## Done

- Pulled origin/main (was at `c0ea1ba`, caught up to `44161bc` — 9 commits).
- Bootstrapped `mac_gui/share-manager/mockups/quality/` 1:1 with Windows:
  - `CHECKLIST.md` ← verbatim copy + §10 macOS-specific rows
    (MAC-1..MAC-8) covering SF Mono / traffic-lights chrome / Keychain /
    `~/.ssh` paths / SMB mount / single-instance Space follow / FDA
    onboarding / Notarized Developer ID
  - `ADR/0001-inspector-light-theme.md` ← verbatim
  - `ADR/0002-modal-header-overflow.md` ← verbatim
  - `ADR/0003-sync-timeline-design.md` ← verbatim
  - `ADR/0004-sync-timeline-narrative.md` ← verbatim
  - `ADR/0005-mac-os-overrides.md` ← new — captures all 8 macOS-only
    decisions in one place
  - `WORKLOG/2026-05-24.md` ← this file

## Plan for the rest of today

Wave A remaining items (in any order — they're independent):

- **T2** clipboard expose — 5 Tauri wrappers + 2 helper fns
  (`read_shared_clipboard`, `write_shared_clipboard`,
  `compressed_images_dir`, `sweep_clipboard_images`)
- **T3** watcher wiring confirm + new `auto_verify_pending` command
- **T1.1** new `src-tauri/src/git.rs` with 17 stub commands; add
  `keyring = { features = ["apple-native"] }` to Cargo.toml; wire
  the stubs through `lib.rs::invoke_handler`. Real bodies land in
  Wave B (T1.2).

Commit convention per MAC_MIRROR_PLAN §7:
- `mac-quality: ...` for everything in this worklog so far
- `mac-t2: ...`, `mac-t3: ...`, `mac-t1-m1: ...` for the rest

## Notes / observations

- The 9 Windows commits include `b9acd22` (trailing-edge debounce
  watcher fix) which we already applied independently on Mac side
  earlier this week — confirmed equivalent after pull, no merge work
  needed.
- ADR-0001..0004 are all OS-agnostic — verbatim copy is the right move,
  no Mac-specific edits required.
- T3 is mostly already done; today's work for T3 is checking the
  `lib.rs::setup` hook + writing the new `auto_verify_pending` command.
- T1 backend is the heaviest skeleton — ~600 LOC of stubs for 17
  commands. Real implementations are Wave B.

## Update — Wave A done, plus catch-up rename to mirror Windows 7358b1a

After landing Wave A (commit `379f012`, pushed), pulled in the sibling
UX-consistency change Windows shipped in `7358b1a` ("English sidebar
labels, log hub below Out, no forced caps"). Not in `MAC_MIRROR_PLAN.md`
tracks but needed before Wave B so both sides render the same labels:

- **Category labels Englishized** (keys & share folders unchanged):
  - Rust side: `mac_gui/share-manager/src-tauri/src/share.rs::CATEGORIES`
  - TS side: `mac_gui/share-manager/src/lib/categories.ts::CATEGORIES`
  - 문서/데이터/코드/리서치/환경설정/빌드/애셋/기타/미분류 →
    Documents/Data/Code/Research/Env/Builds/Assets/Misc/Unclassified
- **Transfer group labels**: "Windows에서 받기" / "Windows로 보낸 것" /
  "받은 기록 (archive)" → "In - from Windows" / "Out - to Windows" /
  "Archive". Mac mirror of Windows's "In - from Mac" / "Out - to Mac".
- **NavGroup field renamed** `label` → `title` so Mac and Windows share
  the same schema (Windows uses `title` in `app.js`). Updated
  `Sidebar.tsx` + `ItemsView.tsx`.
- **Log Hub group** added directly below the Out group (above Memo /
  Clipboard tools) with 5 sub-items: Sent / Received / Errors /
  Compressed images / Worklog. Routes to `panel: "logs"` which renders
  a Wave B placeholder (`LogHubPlaceholder` in `App.tsx`) until T4
  lands the real backend + view.
- **`.nav-group-header`** removed `text-transform: uppercase` from
  `global.css` so labels show in their intended casing (In/Out
  capitalized) rather than ALL CAPS. The `.clip-entry-os` badge keeps
  its intentional uppercase styling.
- Verification: `cargo check` clean, `cargo test --lib` 39/39 pass,
  `npx tsc --noEmit` clean, `npm run build` produces a 217 kB JS
  bundle with no warnings.

## Wave B — T4 + T6 + T1.2 + T1.3 done

Two commits, all four tracks green:

- **`4fc8400` mac-t4-t6** — Log Hub backend (`log_hub.rs` w/ 5 tests),
  LogsView (jsonl streams + compressed gallery), HTML inspector
  pre-flight (`inspect_html_assets` w/ 3 tests + `HtmlInspectorModal`
  + `useSendFlow` gate). +8 cargo tests (39→47).
- **`aae7243` mac-t1-2-t1-3** — Real `git.rs` (`scan_and_publish_git`,
  GitHub API via `ureq`, Keychain PAT under
  service="mac-window-git", account="github-pat", build_repo_graph
  with the §18.4 schema). TS data layer: `gitStore` Context +
  `computeGitNarrative` (8-case verdict table). +4 cargo tests
  (47→51).

## Wave C — T1.4 + T1.7 + T1.5 + T1.6 + T2.5 done

Three commits in sequence, the full v0.3 git UI now visible on Mac:

- **`d46105d` mac-c1** — L1 Dashboard (`GitView` + `RepoCard` +
  `ThreeNodeBridge` + `GitToolbar` + hero stats), Creds UX
  (`TokenSettings` + `SshSettings` in a new Settings → Git section),
  new "🌿 Git" sidebar pinned nav. 7 new components, +5 CSS sections.
- **`3ccae97` mac-c2** — L2 Detail Modal (3 swimlanes per ADR-0002
  grid header) + L3 Raw Inspector (5 tabs: Raw Diffs / Daemon Logs /
  Git Config / All Commits / Sync Timeline). Sync Timeline panel is a
  direct React port of `gitTimelineSVG` — lane bands + LCA marker +
  shared spine + brand-color tip pills, dot onClick routes through
  React state instead of DOM mutation. ~380 lines of CSS for both
  modals (light theme per ADR-0001).
- **C3 (this commit) — T2.5 + T8** — Shared clipboard panel at top of
  ClipboardView (sticky-note style `current.json` r/w with rolling
  50-entry history), sort toggle 최신순 / 호스트별. CHECKLIST §10
  marked all ✅, Phase 1 진행 표 status flipped from "pending verify"
  → "implemented" with concrete commit hashes + CSS selectors.

## Wave totals

| | Wave A | Wave B | Wave C |
|---|---|---|---|
| Commits | `379f012` + `ef3b9cc` | `4fc8400` + `aae7243` | `d46105d` + `3ccae97` + (C3) |
| New tests | 39 | +12 (51 total) | unchanged (no new backend) |
| LOC delta (approx) | ~3000 | ~2600 | ~3800 |
| Last bundle | 217 kB JS / 21 kB CSS / 71 modules | 224 kB / 24 kB / 73 modules | 264 kB / 44 kB / 84 modules |

Mac side is now at feature parity with the Windows v0.3 git
dashboard. Open items for future sessions are out of scope for the
MAC_MIRROR_PLAN handoff — they live in their own backlog
(automated git scan trigger on share-changed, settings.git.only_mine
filter, AppearanceSection icon themes catalog, etc.).

## v0.3.0 release ship (Phase 1 — docs + version bump)

Wave A/B/C 가 끝난 후 사용자가 "동기화 완료된 거야?" 질문 → 코드 parity
audit 결과 **데이터/UI/invoke surface 는 100% mirror 완료** 확인. 단
3가지 미수행:
1. cross-OS 라이브 통합 테스트
2. UI 시각 검수
3. v0.3.0 릴리스 빌드 (현재 0.2.4)

이 세 작업을 마무리하기 위해 4-phase 마스터 계획 수립
(`/Users/chan/.claude/plans/adaptive-brewing-pearl.md`).

### Phase 1 산출물 (Claude — 이 커밋)

- **`mockups/quality/INTEGRATION_TEST.md`** — 12개 cross-OS 시나리오
  (파일 전송 양방향, shared clipboard, streaming clipboard + 이미지
  dedup, notes last-write-wins / 동시 create, git data publish,
  PAT round-trip, HTML pre-flight, Log Hub sync, offline reconnect).
  각 행 Steps / Expected / Verify / Pass-Fail / Observed 칸.
- **`mockups/quality/UI_VISUAL_AUDIT.md`** — CHECKLIST 모든 implemented
  행 + MAC-1~MAC-8 을 측면-비교 캡처 프로토콜로 재작성. `cargo tauri
  dev` 한 번 띄워 12 view 순회 walk-order. §18.7 토큰별 비교 항목.
- **`mockups/quality/RELEASE_RUNBOOK.md`** — Phase 4 step-by-step.
  섹션 A (one-time notary setup, 9 단계 — App Store Connect API key
  생성 → .p8 배치 → ASC_API_KEY_ID/ISSUER_ID export →
  setup-notary.sh) + 섹션 B (release execution, 9 단계 — 사전 검증 →
  태그 → release.sh → 산출물 확인 → Gatekeeper 검증 → push commit+tag
  → gh release create → updater endpoint 검증 → 사후 체크).
- **version bump**: `tauri.conf.json` + `package.json` 모두 0.2.4 →
  0.3.0.
- **`RELEASES.json` prepend**: v0.3.0 entry (8개 highlights — git
  대시보드 / Log Hub / sticky clipboard / HTML inspector / PAT+SSH
  Settings / Sync Timeline narrative / 사이드바 영어화 / 테스트 51).
- **이 WORKLOG 섹션 append**: Phase 1 산출 정리.

### 다음 단계 (Phase 2~4 — User 액션 필요)

| Phase | 누가 | 무엇 |
|---|---|---|
| 2.1 | User | A.1~A.5 (notary setup, 5분) |
| 2.2 | User + Claude triage | INTEGRATION_TEST.md 12 행 실행 (양 머신) |
| 3 | User + Claude patches | UI_VISUAL_AUDIT.md 캡처 + drift 패치 |
| 4 | User (또는 Claude 인가시) | release.sh + gh release |

bump commit SHA / 산출물 4종 / GitHub release URL 은 Phase 4 완료 후
이 섹션에 채워넣음.
