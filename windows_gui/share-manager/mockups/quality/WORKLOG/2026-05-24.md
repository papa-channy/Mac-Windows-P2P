# 작업 로그 — 2026-05-24

## 사용자 피드백 (스크린샷 검토)
- Inspector 버튼이 여전히 모달 밖으로 잘려 표시됨 (image 15) — 여러 차례 수정 후에도 재발
- Inspector 탭(Layer 3)만 다크 테마인 이유 불분명 — 앱 전체가 라이트인데 일관성 깨짐
- Sync Timeline 그래프 완성도 부족: "chans-MacBook-Pro" 라벨 잘림, 그래프가 작게 압축, 디자인 빈약
- 사용자 요청: 컴포넌트별 체크리스트 + 통과 기준 + ADR + 작업 로그로 체계화

## 프레임워크 셋업 (이번 turn)
1. `mockups/quality/CHECKLIST.md` 작성 — Layer 1/2/3 전체 컴포넌트 분류 + UPC 9개 + 개별 통과 기준 + 상태 표
2. `mockups/quality/ADR/0001-inspector-light-theme.md` — Inspector 라이트 통일
3. `mockups/quality/ADR/0002-modal-header-overflow.md` — 모달 헤더 오버플로 영구 해결
4. `mockups/quality/ADR/0003-sync-timeline-design.md` — Sync Timeline 재설계
5. `mockups/quality/WORKLOG/2026-05-24.md` — 이 파일

## Phase 1 — ADR 적용 (이번 turn)
- [x] CHECKLIST 작성
- [x] ADR 3개 작성
- [x] 작업 로그 시작
- [x] ADR-0001 적용 — `.git-inspector-window` 및 자식 CSS 전부 라이트로 재작성, 모든 다크 규칙을 `!important` 라이트로 덮어쓰기
- [x] ADR-0002 적용 — 모달 헤더 grid `minmax(0,1fr) auto auto auto`, `.git-detail-controls { display: contents }`로 직접 grid item으로, branch select max-width 200 + title tooltip
- [x] ADR-0003 적용 — `gitTimelineSVG()` 재작성: 220px 라벨 영역, 86px 레인 높이, 28×28 브랜드 아이콘 타일 (인라인 SVG path), tip pill에 SHA 포함, LCA 라벨 박스화, 하단 legend
- [x] 빌드·배포 (`release` 2m 58s)
- [x] 체크리스트 상태 ❌→⏳ pending verify (시각 확인 후 ✅로 전환 예정)
- [x] 커밋·푸시

## Phase 1 산출물
- `style.css`: "ADR-0001~0003" 마커 블록 단일 위치, 모든 다크 규칙 덮어쓰기. 약 +280줄.
- `app.js`: `gitTimelineSVG` 재작성 (220px 라벨, 라이트 팔레트, 브랜드 SVG 인라인). branch select에 title attr.
- 문서: CHECKLIST, ADR 3개, 이 worklog.

## 사용자 시각 검증 대기 항목
1. Inspector 헤더 — 라이트 (`var(--surface-low)`), Back 버튼은 라이트
2. 사이드바 탭 — 라이트, 활성 탭 sky 좌측 stripe
3. Raw Diffs — `+`=#1F883D / `-`=#CF222E / `@@`=#0969DA, 코드 베이스 `#F6F8FA`
4. Daemon Logs — pill 색상 (rose/emerald/sky/violet)
5. Git Config — 라이트 코드 베이스
6. All Commits — 날짜 wrap 안 됨, hover surface-2
7. Sync Timeline — 220px 라벨 영역에 "chans-MacBook-Pro" 완전 표시, 브랜드 아이콘 타일, legend
8. 모달 헤더 — 1280/1100/900px 다 viewport 안에 Inspector + close X 보임

## Phase 1 확인 결과 (사용자 image 22)
- L3-A~F (Inspector 헤더·사이드바·Diffs·Logs·Config·Commits): ✅ 라이트 통일 OK
- L3-G (Sync Timeline): ⏳ 그래프 잘 그려지나 상단 다크 띠 잔재 + tip pill 우측 잘림 + narrative 빈약
- L2-A (모달 헤더): ✅ Inspector + close 잘림 없음
- 추가 지적: Inspector 버튼이 검은 fill — 라이트 톤으로

## Phase 1.5 (이번 turn) — ADR-0004 + Inspector 버튼 라이트
- ADR-0004 작성 — Sync Timeline narrative 3-panel 설계
- `gitTimelineSVG`: padR 240 → 360 (tip pill 잘림 해결), 점에 `data-sha` (클릭→Panel 3)
- `renderGITimeline` 재작성 — 3-panel:
  - Panel 1 **Status Summary**: 진단(verdict + icon + sub) + 호스트 3행(아이콘+이름+sha+chips) + 권장 액션 카드
  - Panel 2 **Graph**: 헤더(repo·branch·hint) + SVG river
  - Panel 3 **Selected Commit**: 클릭한 커밋의 SHA·메시지·저자·날짜·존재 소스 pill
- `computeGitNarrative()` 추가 — verdict 규칙 표(ADR-0004 §"권장 액션 규칙") 코드화
- CSS `.gtl-*` 신규 클래스 (status / row / chip / action / graph / detail / src-pill)
- 다크 잔재 제거: `.gi-timeline`, `.gi-tl-head`, `.gi-tl-river` 모두 `background: transparent !important`
- Inspector 버튼: dark fill → 라이트 (white bg + dark text + accent terminal icon, hover시 accent border)

## 결과
체크리스트:
- L3-G: ⏳ pending verify → 검증 대기 (narrative 3-panel 적용)
- L2-A Inspector 버튼: ⏳ pending verify → 라이트 적용

## 다음 turn 예정
- 시각 검증 결과 받기 — Status Summary 가독성, 그래프 잘림 0, Panel 3 클릭 동작
- 미통과 항목 발견 시 ⏳ → ❌ 또는 ✅
- L1-C 3-node bridge 디테일 (Apple 로고 검정인지 흑백인지 등)
- L2-C 스윔레인 빈 상태 일관성
- UPC 1~9 전수 점검

## 다음 turn 예정
- L1-C 디테일 (3-node Bridge 미세조정)
- L2-C 스윔레인 빈 상태 일관성
- UPC 1~9 전수 검증

## Phase 2 — L1 카드 unified layout (사용자 피드백 image 1)

### 사용자 피드백
- 충돌 임박 카드만 디자인 깨짐: 카드 전체 핑크 배경 + 좌측 두꺼운 stripe + 3-node bridge 카드 밖으로 잘림
- 다른 카드(발산/동기화)는 정상
- 사용자 요청: "상황별 다르게 카드내 컴포넌트 배치를 처리하지 말고 모두 동일하게 처리"
- 헤더 🌿 아이콘 어색
- 메트릭 카드 여백 / 정렬 완성도 부족

### 작업
1. mockup dashboard-v2.html 작성 (Image #5 양식: 위(헤더+chip) → 아래(큰 bridge box))
2. mockup detail-v2.html 작성 (3-lane + 절대위치 connector chip)
3. **ADR-0005** 작성 — L1 Card Unified Layout: stripe / conflict 별도 div 제거, 모든 카드 같은 클래스 + chip 색만 변경
4. 사용자 OK ("이대로 프로그램에 적용해보자")
5. `app.js` `renderGitL1Card`:
   - 라인 1592의 `git-card-stripe` 조건부 div 제거
   - 라인 1610~1614의 `git-card-conflict` 조건부 div 제거
   - 모든 verdict가 동일 구조로 렌더링
6. `index.html`: `<h2>🌿 Git 현황</h2>` → Lucide git-branch SVG (color: #1F883D)
7. `style.css`: 맨 끝에 "ADR-0005" 마커 블록 추가 (250줄)
   - `.git-card-stripe { display: none !important }`, `.git-card-conflict { display: none !important }` (예방용)
   - `.git-card` flex column + padding 18/20 + border-radius 14 + gap 14
   - `.git-card-bridge` surface-2 inner box + grid 1fr auto 1fr auto 1fr auto
   - `.gn-icon` 44×44 + led 11px
   - `.git-card-badge-*` chip 색 통일 (GitHub-light: #1F883D / #BF8700 / #B11C26)
   - `.git-hero-card` metric 양식 (38×38 icon tile, 26px num)
   - `.git-page-title` git-branch icon flex-inline 스타일
8. robocopy: repo → 작업본 (`D:\Mac-Window_Share\...`) 동기화
9. cargo build --release (in progress)

### 검증 대기 항목 (Phase 2)
- [ ] 충돌 카드도 발산/동기화 카드와 100% 동일 layout
- [ ] 카드 모두 흰 배경, 좌측 stripe 0
- [ ] bridge box 카드 안에 surface-2 inner box
- [ ] node icon 44×44, led 우하단 dot
- [ ] 헤더 git-branch 아이콘 emerald
- [ ] hero metric 3개 같은 높이, 38×38 icon tile
- [ ] verdict chip 색만 다르고 layout 동일

### 다음 turn 예정
- 빌드 완료 후 사용자 시각 검증 → ⏳ 항목들 ✅ 전환
- 추가 polish 항목 발견 시 작업
- 만족 시 → 커밋·푸시 + L2 detail polish 검토

## Phase 2.1 — 충돌 카드 폭 fix (사용자 피드백 image #6)
- 빌드 후 image #6 확인: 충돌 카드의 bridge box가 카드 폭의 절반만 차지 (좌측 정렬)
- 원인: 기존 `.git-card-conflict { align-items: center }` (style.css:1322) 가 modifier로 카드에 붙으면서 cross-axis(가로) center 정렬 → 자식 width auto로 줄어듦
- 수정:
  - `.git-card-conflict` 기존 3블록 (1041 / 1322 / 1918) 완전 제거 (modifier로 쓰면 카드 자체 폭 줄였음)
  - `.git-card` 에 `align-items: stretch !important` + `width: 100% !important` 안전망 추가
  - ADR-0005 블록의 `.git-card-conflict { display: none }` / `.git-card-stripe { display: none }` 제거 (modifier로 쓰니 의미 혼란)
- robocopy + 재빌드 (1m 48s) + bin/ swap 완료

## Phase 3 — L2 Detail Modal mockup-v2 정렬 (사용자 피드백 image #7 / mockup image #8)

### 사용자 피드백
- L2 detail modal이 mockup detail-v2.html 양식과 어긋남:
  1. 헤더 아래 verdict 진단문 누락 (현재 작은 chip만)
  2. lane 사이 connector chip ("동기화" / "Win ↑ 2") 없음
  3. 푸터 "Mac · 동기화 · Origin · 동기화 · Win" 이 호스트 연결 시각화 중복 (verdict + lane 헤더에서 이미 표현)

### 작업
1. **ADR-0006** 작성 — L2 Detail Modal mockup-v2 alignment
2. `app.js` `renderGitL2Lanes` 수정:
   - `$gitDetailSummary.innerHTML` — 큰 verdict chip + 진단문 (가독성 강화)
   - `$gitDetailBody.innerHTML` — 3 lane shell에 absolute-positioned connector chip 2개 추가
   - 푸터 교체: `gitConnectorBar()` → 새 footer (메타 3 항목 + 액션 버튼 3)
3. `app.js` 헬퍼 함수 신규:
   - `gitL2Verdict(ctx)` — verdict-action 매핑 ({ kind, title, head, desc })
   - `gitL2Connector(host, ahead, behind)` — connector chip 데이터 ({ kind, icon, text })
4. `style.css` 끝에 ADR-0006 마커 블록 추가 (~180줄):
   - `.git-l2-connbar { display: none !important }` (legacy)
   - `#git-detail-summary` flex layout + verdict chip / verdict text 스타일
   - `.git-l2-shell { position: relative }` + `.git-l2-connector.c1/c2` 절대 위치
   - `.git-l2-connector-line` 점선 + `.git-l2-connector-chip` 부착
   - `.git-l2-footer` (flex space-between) + `.git-l2-footer-meta` + `.git-l2-footer-actions`
   - `.git-l2-btn` + `.git-l2-btn.primary` (sky)

### 검증 대기 항목 (Phase 3)
- [ ] verdict-row: 큰 chip + 진단문 ("Win이 origin보다 N 커밋 앞섬 · Mac은 깨끗하고 동기화됨...")
- [ ] lane 사이 connector chip 2개 ("동기화" / "Win ↑ N") 표시
- [ ] 푸터 = 메타(스캔 시각 / 커밋 수 / 공통 조상) + 액션(전체 커밋 / DAG / Sync 실행)
- [ ] 1280 / 1100 / 900px 모든 폭에서 잘림 0
- [ ] verdict 케이스별 색상 (synced/diverged/conflict/partial) 정확

### 다음 turn 예정
- 시각 검증 결과 받기
- 만족 시 → ✅ 전환 + 커밋·푸시
- 액션 버튼 (전체 커밋 보기 / DAG 보기 / Sync 실행) 핸들러 연결은 다음 단계
